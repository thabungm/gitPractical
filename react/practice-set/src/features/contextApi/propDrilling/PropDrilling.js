import Child from './Child';
import ParentComponent from './Parent';

const PropDrilling = () => {
  return (
    <div>
      <ParentComponent theme={'blue'} />
    </div>
  );
};

export default PropDrilling;
